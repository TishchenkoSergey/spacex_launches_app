name: spacex_launches_app
description: spacex_launches_app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  bloc: ^9.0.0
  go_router: ^16.2.1
  get_it: ^8.2.0
  injectable: ^2.5.1

  domain:
    path: ./packages/domain

  data:
    path: ./packages/data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  very_good_analysis: ^9.0.0
  build_runner: ^2.7.1
  flutter_gen_runner: ^5.8.0
  injectable_generator: ^2.8.0

flutter:
  uses-material-design: true
  generate: true
